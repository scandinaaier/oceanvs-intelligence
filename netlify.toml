[build]
  command = "npm run build"
  publish = "dist"

# React SPA routes — rewrite to app.html so React Router takes over.
[[redirects]]
  from = "/login"
  to = "/app.html"
  status = 200

[[redirects]]
  from = "/dashboard/*"
  to = "/app.html"
  status = 200
